import { connect } from 'react-redux';
import RecordPsychScreenComponent from './RecordPsychScreen';

const mapStateToProps = state => ({
  
});

const mapDispatchToProps = dispatch => ({
  
});

export const RecordPsychScreen = connect(
  mapStateToProps,
  mapDispatchToProps
)(RecordPsychScreenComponent);