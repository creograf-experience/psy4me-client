import { connect } from 'react-redux';
import PaymentScreenComponent from './PaymentScreen';

const mapStateToProps = state => ({
  
});

const mapDispatchToProps = dispatch => ({
  
});

export const PaymentScreen = connect(
  mapStateToProps,
  mapDispatchToProps
)(PaymentScreenComponent);